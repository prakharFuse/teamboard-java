<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.appfire.resolver</groupId>
  <artifactId>teamboard-java</artifactId>
  <version>1.0.0</version>
  <packaging>jar</packaging>

  <properties>
    <maven.compiler.release>17</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <junit.version>5.10.2</junit.version>
    <jackson-databind.version>2.13.4.2</jackson-databind.version>
  </properties>

  <!--
    FIXTURE INVARIANT (j90, resolver-core spec 015 — dependency-fix Java path):
    This project declares ONLY jackson-dataformat-yaml:2.13.2, which pulls
    `com.fasterxml.jackson.core:jackson-databind:2.13.2` TRANSITIVELY. That
    version is vulnerable (GHSA-57j2-w4cx-62h2 / CVE-2022-42003, fixed 2.13.4.2).

    The vulnerable artifact is NOT declared here — so the correct, deterministic
    remediation is a Maven dependency-management PIN of it to a patched version
    (Maven's mechanism to force a transitive version), NOT an edit to the
    dependencies below. Keeping main on the vulnerable transitive version is the
    point of the fixture; the dependency-fix resolve adds the pin on a PR branch.
  -->
  <dependencyManagement>
  <dependencies>
      <dependency>
        <groupId>com.fasterxml.jackson.core</groupId>
        <artifactId>jackson-databind</artifactId>
        <version>2.13.2.1</version>
      </dependency>
    </dependencies>
</dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>com.fasterxml.jackson.dataformat</groupId>
      <artifactId>jackson-dataformat-yaml</artifactId>
      <version>2.13.2</version>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.2.5</version>
      </plugin>
    </plugins>
  </build>
</project>
